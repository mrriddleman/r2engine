include "AssetRef.fbs";

namespace flat;

file_identifier "sdef";
file_extension "sdef";

table SoundDefinitions
{
	version:uint;
	masterBank:AssetRef;
	masterBankStrings:AssetRef;
	banks:[AssetRef];
}

root_type SoundDefinitions;